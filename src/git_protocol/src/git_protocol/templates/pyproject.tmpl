[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "{repo_name}"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[project.scripts]
git-protocol = "src.git_protocol.cli:main"
